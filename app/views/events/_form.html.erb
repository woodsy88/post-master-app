<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :action %>
    <%= form.text_field :action %>
  </div>

  <div class="field">
    <%= form.label :eventable_id %>
    <%= form.number_field :eventable_id %>
  </div>

  <div class="field">
    <%= form.label :eventable_type %>
    <%= form.text_field :eventable_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
